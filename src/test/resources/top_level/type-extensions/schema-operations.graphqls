# The Root Query for the application

extend type QueryType {
    # Get User Goals
    person : PersonInterface
    firstEmployedPerson:  EmployedPersonsUnion
    secondEmployedPerson:  EmployedPersonsUnion
}

extend type MutationType {
    # Get User Goals
    addTeacher(teacher : InputTeacher):  Teacher
    addActor(newActor : InputActor):  Actor
}

