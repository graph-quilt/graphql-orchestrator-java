type Query {
    employeeById(id: ID!): Employee
}

type Employee @key(fields: "id"){
    id: ID!
    username: String!
    password: String!
}

type Person {
    id: ID
    firstName: String
    lastName: String
}